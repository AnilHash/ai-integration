name: Eval Gate

on:
  pull_request:
    branches: [master]

jobs:
  golden-set-eval:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    services:
      qdrant:
        image: qdrant/qdrant:latest
        ports:
          - 6333:6333
    steps:
      - uses: actions/checkout@v4

      - name: Wait for Qdrant
        run: |
          for i in $(seq 1 30); do
              curl -s http://localhost:6333/healthz > /dev/null && break
              sleep 1
          done

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0
        with:
          enable-cache: true

      - name: Install Python dependencies
        run: uv sync --locked

      - name: Cache Ollama models
        uses: actions/cache@v4
        with:
          path: ~/.ollama/models
          key: ollama-qwen2.5-1.5b

      - name: Install Ollama
        run: curl -fsSL https://ollama.com/install.sh | sh

      - name: Start Ollama and pull model
        run: |
          ollama serve &
          for i in $(seq 1 30); do
              curl -s http://localhost:11434/api/tags > /dev/null && break
              sleep 1
          done
          ollama pull phi3:mini

      - name: Seed Qdrant with golden-set corpus
        run: uv run python -m scripts.load_chunks

      - name: Run golden set eval
        timeout-minutes: 45
        env:
          OLLAMA_MODEL: phi3:mini
          LANGFUSE_PUBLIC_KEY: ${{ secrets.LANGFUSE_PUBLIC_KEY }}
          LANGFUSE_SECRET_KEY: ${{ secrets.LANGFUSE_SECRET_KEY }}
          LANGFUSE_HOST: ${{ secrets.LANGFUSE_HOST }}
        run: uv run python -m eval.run_eval
